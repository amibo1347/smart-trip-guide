# 작업 인수인계 — Smart Travel Planner (2026-06-03 16:15)

> 이 파일 하나만 읽고 이어서 작업할 수 있도록 정리한 핸드오프 문서입니다.
> 새 Claude Code 세션은 **먼저 이 문서를 읽고**, 아래 "재개 체크리스트"부터 확인하세요.

---

## 0. 프로젝트 한눈에

- **무엇**: 여행 계획·기록·복기를 잇는 PWA. 백엔드 Spring Boot(Java 21, Gradle), 프론트 React+Vite(PWA), DB MySQL 8(Docker).
- **핵심 흐름(탭)**: 여행 상세 = `📋 일정(여행 전)` / `📍 기록(여행 중)` / `📊 복기(여행 후)`.
- **작업 디렉터리**: `C:\bootcamp\smart-travel-planner`
- **OS/셸**: Windows 11 / PowerShell 7. (Bash 툴도 사용 가능, cwd가 `frontend/`로 남아있을 수 있으니 절대경로 주의)
- **포트**: 백엔드 `:8082`, 프론트 dev `:5173`, MySQL 호스트 `:3308`→컨테이너 3306.

### 실행 방법
```powershell
# (1) MySQL 컨테이너 (Docker Desktop이 켜져 있어야 함)
docker compose up -d
# (2) 백엔드 — .env 로드 + Flyway + (gradle가 프론트 빌드→static 복사까지 자동) → http://localhost:8082
.\dev.ps1
# (3) 프론트 핫리로드로 개발 시 (선택)
cd frontend; npm run dev   # http://localhost:5173 (api/uploads는 8082로 프록시)
```
- `dev.ps1`이 `.env`(GEMINI_API_KEY 등)를 로드하고 `gradlew bootRun` 실행. **백엔드만 띄워도** `processResources→copyFrontend→frontendBuild`로 프론트가 같이 빌드되어 `src/main/resources/static`에 번들됨 → `:8082` 한 곳에서 화면까지 서빙.
- **현재 상태**: 백엔드는 백그라운드로 실행 중(로그 `bootrun.log`). 종료/재시작은 PowerShell에서 `SmartTravelPlanner` 포함 java 프로세스 Stop-Process 후 `.\dev.ps1`.

### ⚠️ 자주 헷갈리는 함정 (중요)
1. **PWA 서비스워커 캐시**: 프론트를 바꿔도 브라우저가 옛 번들을 캐시해서 안 바뀐 것처럼 보임. 변경 확인 시 항상 **F12 → Application → Service Workers `Unregister` + `Clear site data` → Ctrl+Shift+R**.
2. **Flyway는 톰캣 포트 바인딩 *전에* 실행됨**. 옛 인스턴스가 8082를 점유한 채 새 인스턴스를 띄우면 → 새 인스턴스가 **마이그레이션만 적용하고 포트 충돌로 죽음** → "DB는 새 스키마인데 실행 중인 코드는 옛 코드"라는 불일치 발생(404/500). 재시작 전 반드시 옛 JVM을 먼저 종료.
3. **JPA `ddl-auto: validate`**: 엔티티와 스키마가 정확히 일치해야 기동됨(스키마 소유권은 Flyway). 엔티티 추가/변경 시 마이그레이션도 같이.
4. **GEMINI_API_KEY**는 `.env`로 주입. `.dev.ps1` 없이 맨 `gradlew bootRun`으로 띄우면 AI 기능 비활성.

---

## 1. 이번 세션에서 끝낸 작업 (전부 ✅, 단 **미커밋**)

마지막 커밋: `4eb22ce`(예약 가져오기). **이번 세션 변경은 아직 커밋 안 됨** — 아래 git 상태 참고. 필요 시 사용자에게 커밋 의향을 물어볼 것.

### A. 정밀 예산 검증 (계획 단계)
- 신규 `budget` 패키지: `GET /api/trips/{id}/budget` → **활동비 합계(plan estCost) + 확정 예약 합계(booking price) vs 예산 한도**, 초과액/남은예산/초과여부 계산. DB 값만 합산(키 불필요, 환각 없음).
- 파일: `budget/controller/BudgetController.java`, `budget/service/BudgetService.java`, `budget/dto/BudgetSummaryResponse.java`
- 프론트: `Itinerary.jsx`의 `BudgetPanel`(예약 카드 아래). 항목/예약 변경 시 `tick` 트리거로 자동 갱신.

### B. 숙박 탭 제거
- `Itinerary.jsx`에서 🏨 숙박 섹션 + `AddAccForm` + 관련 state/`api` 제거(확정 예약이 항공/숙소를 모두 커버).
- **백엔드 Accommodation API(`/api/.../accommodations`)는 그대로 남아있음**(미사용). 추후 정리 가능.

### C. AI 일정 — 실제 상호 + 1인 객단가 자동 추정
- `AiPlanService.buildPrompt` 보강: MEAL/SPOT/ACTIVITY에 **실제 상호명** + **1인 평균 객단가(원화)** 추정(예: 이치란 라멘 1인 ≈ 12000).
- **생성 시 자동 채움**: `generate()`가 저장 직후 `fillMissingCosts()` 호출 — 비용이 빈(null/0) MEAL/SPOT/ACTIVITY 항목만 모아 **1회 Gemini 호출**로 보강(이미 다 채웠으면 추가 호출 없음, 실패해도 일정은 유지). → 별도 "AI로 채우기" 버튼은 **제거함**.

### D. 일정 항목 인라인 편집
- `PATCH /api/plan-items/{id}` (`UpdatePlanItemRequest`: title/plannedStart/plannedEnd/estCost). `PlanItem.update()`, `PlanService.updateItem()`.
- 프론트: `Itinerary.jsx` 각 항목 **✎ 버튼 → `EditItemForm`** 인라인. 저장 시 예산 점검 즉시 갱신.

### E. 여행 완전 물리 삭제
- (소프트삭제로 먼저 했다가 **물리삭제로 변경**.) `V12` 마이그레이션이 모든 trip 참조 FK를 `ON DELETE CASCADE`로 재정의 → `DELETE FROM trips`가 일정·예약·기록까지 연쇄 삭제.
- `DELETE /api/trips/{id}`, `TripService.delete()`=물리삭제. 프론트: `App.jsx` 내 여행 카드 **✕**(stopPropagation+확인).

### F. 기록(여행 중) — 3영역 → 단일 "기록"으로 통합 + 사진 핀 지도
- **데이터 완전 대체**: `location_logs`/`mood_logs`/`expenses` 3테이블 **삭제** → **`trip_moments` 단일 테이블**(언제/어디서/기분/금액/메모/사진, 전부 nullable). 마이그레이션 `V12`.
- 엔티티/DTO/리포: `tracking/entity/TripMoment.java`, `repository/TripMomentRepository.java`, `dto/MomentRequest.java`, `dto/MomentResponses.java`(MomentResponse, MomentSummary). **옛 Expense/LocationLog/MoodLog 및 그 repo/dto 삭제됨**.
- 서비스/컨트롤러 재작성: `TrackingService`, `TrackingController`.
  - `POST /api/trips/{id}/moments` (multipart: `data`=JSON 파트 + `photo`=이미지 파트, 선택)
  - `POST /api/trips/{id}/moments` (application/json: 사진 없는 오프라인 큐 재전송 경로)
  - `GET /api/trips/{id}/moments`, `GET .../moments/summary`, `DELETE .../moments/{momentId}`(사진 파일까지 삭제)
- **사진 저장 = 서버 로컬 파일**: `PhotoStorageService`(저장/삭제, 경로이탈 방지). 디렉터리 `app.upload-dir`(기본 `./uploads`). `WebConfig`가 `/uploads/**` 정적 서빙. `application.yml`에 multipart 한도(10MB) 추가. `vite.config.js` 프록시에 `/uploads` 추가. `.gitignore`에 `uploads/`.
- 복기(Review) 재배선: `ReviewService`가 expenses/locations/moods 대신 **moments** 사용(지출합계/카테고리/위치/기분수). `ReviewResponse.LocationPoint`에 `mood/amount/memo/photoUrl` 추가.
- **지도 = 사진 일기장**: `Tracking.jsx`(+`Review.jsx`) Leaflet 지도에 위치별 **사진 썸네일 핀(DivIcon)**, 없으면 기분 이모지 점. 핀 클릭 시 사진/시간/기분/금액/메모 팝업. CSS `.photo-pin/.pp-img/.pp-dot/.pp-tip`, `.m-thumb` 등 `index.css`에 추가.
- 프론트 `Tracking.jsx` 완전 재작성: 상단 지도 + **한 장의 "✍️ 기록하기" 카드**(현재위치 잡기 → 기분 선택 → 지출(선택) → 메모(선택) → 사진(선택) → 저장) + 최근 기록 리스트(각 항목 **✕ 삭제**).
  - 오프라인: 사진은 큐 불가 → 텍스트만 JSON 큐로 적재 후 복귀 시 동기화. 온라인은 multipart.

---

## 2. 현재 git 상태 (미커밋)

신규(`??`):
- `src/main/java/com/travel/planner/budget/`(controller/service/dto)
- `planning/dto/UpdatePlanItemRequest.java`
- `tracking/dto/MomentRequest.java`, `tracking/dto/MomentResponses.java`
- `tracking/entity/TripMoment.java`, `tracking/repository/TripMomentRepository.java`, `tracking/service/PhotoStorageService.java`
- `src/main/resources/db/migration/V12__trip_moments_and_cascade.sql`

수정(`M`): `.gitignore`, `application.yml`, `WebConfig.java`, `ReviewResponse.java`, `ReviewService.java`, `PlanController.java`, `PlanItem.java`, `AiPlanService.java`, `PlanService.java`, `TrackingController.java`, `TrackingService.java`, `TripController.java`, `TripService.java`, 프론트 `App.jsx/Bookings.jsx/Itinerary.jsx/Review.jsx/Tracking.jsx/api.js/index.css/vite.config.js`

삭제(`D`): tracking의 `Expense/LocationLog/MoodLog` 엔티티·리포·`ExpenseRequest/LocationRequest/MoodRequest/TrackingResponses` DTO.

> DB 상태: **V12까지 적용 완료**(flyway_schema_history version=12 success). 옛 3테이블 없음, `trip_moments` 존재, 8개 FK 모두 CASCADE 확인됨.

---

## 3. 재개 체크리스트 (새 세션 시작 시)

1. `docker compose up -d`로 MySQL 확인(`docker inspect --format '{{.State.Health.Status}}' smart-travel-mysql` = healthy).
2. 8082 점유 프로세스 확인 → 옛 JVM 떠 있으면 **먼저 종료** 후 `.\dev.ps1`.
3. 빌드 검증: 백엔드 `.\gradlew.bat compileJava` / 프론트 `cd frontend; npm run build`.
4. 엔드포인트 살아있는지(인증 전 401이 정상): `curl -s -o NUL -w "%{http_code}" http://localhost:8082/api/trips/1/moments` → 401.
5. 화면 확인 전 **PWA 캐시 초기화**(위 함정 1).

---

## 4. 주요 파일 지도 (빠른 진입점)

| 기능 | 백엔드 | 프론트 |
|---|---|---|
| 예산 점검 | `budget/**`, `Trip.budgetLimit`, `PlanItem.estCost`, `TripBooking.price` | `Itinerary.jsx`(BudgetPanel) |
| AI 일정 | `planning/service/AiPlanService.java`, `ai/GeminiClient.java` | `Itinerary.jsx`(AiGenerate) |
| 일정 항목 | `planning/controller/PlanController.java`, `PlanService.java` | `Itinerary.jsx`(EditItemForm/AddItemForm) |
| 확정 예약 | `booking/**` | `Bookings.jsx` |
| 통합 기록 | `tracking/**`(TripMoment, TrackingService/Controller, PhotoStorageService) | `Tracking.jsx` |
| 복기 | `feedback/**`(ReviewService) | `Review.jsx` |
| 여행/삭제 | `trip/**` | `App.jsx`(TripSection) |
| 설정 | `config/SecurityConfig`(`/api/**` 인증, CSRF off), `config/WebConfig`(CORS + `/uploads/**`) | `api.js`, `vite.config.js` |

마이그레이션: `src/main/resources/db/migration/V1..V12`. (V12가 이번 세션 추가.)

---

## 5. 알려진 한계 / 다음에 할만한 것

- **오프라인 + 사진**: multipart라 오프라인 큐에 사진을 못 담음(텍스트만 큐). 필요 시 IndexedDB 기반 큐로 승격.
- **/uploads/** 공개 경로**: 파일명이 UUID라 추측은 어렵지만 인증 게이트는 없음(개인용 수준). 강화하려면 별도 인증 서빙.
- **Accommodation 백엔드 잔존**: 프론트에선 제거됐지만 API/엔티티/테이블 남아있음 → 정리 가능.
- **소프트삭제 컬럼**: 모든 테이블에 `deleted/deleted_at` 있으나 현재 미사용(여행은 물리삭제로 전환). BaseEntity에 매핑은 남아있음.
- 커밋 미실시: 이번 세션 전체가 working tree에만 있음 → 적절한 단위로 커밋 권장(사용자 승인 후).

---

## 6. 검증/디버그 한 줄 명령

```bash
# DB 스키마/마이그레이션 상태
docker exec smart-travel-mysql mysql -uroot -proot smart_travel -N -e "SELECT version,success FROM flyway_schema_history ORDER BY installed_rank DESC LIMIT 3;"
docker exec smart-travel-mysql mysql -uroot -proot smart_travel -N -e "SHOW TABLES;"
# FK CASCADE 확인
docker exec smart-travel-mysql mysql -uroot -proot smart_travel -N -e "SELECT TABLE_NAME,CONSTRAINT_NAME,DELETE_RULE FROM information_schema.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_SCHEMA='smart_travel';"
# 백엔드 기동 로그
tail -30 bootrun.log
```
