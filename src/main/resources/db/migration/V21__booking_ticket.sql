-- =====================================================================
-- V21: 확정 예약에 '예약 확인증' 파일(항공권 e-티켓·숙소 바우처 등) 첨부.
--  image_url 은 대표 이미지(썸네일), ticket_url 은 실제 제시용 문서(이미지 또는 PDF)로 역할이 다르다.
-- =====================================================================

ALTER TABLE trip_bookings
    ADD COLUMN ticket_url VARCHAR(500) NULL AFTER image_url;
