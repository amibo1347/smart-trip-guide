-- =====================================================================
-- V23: 프로필 아바타.
--  "preset:xxx"(기본 제공 아바타) 또는 "/uploads/..."(직접 올린 사진).
--  null 이면 기본 캐릭터 아바타.
-- =====================================================================

ALTER TABLE users
    ADD COLUMN avatar VARCHAR(500) NULL AFTER default_origin;
