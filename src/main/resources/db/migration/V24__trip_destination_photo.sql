-- =====================================================================
-- V24: 여행에 목적지 대표 사진 저장.
--  목적지를 고를 때 그 장소의 사진(Google Places 프록시 URL)을 함께 저장해,
--  여행 카드/요약을 사진으로 예쁘게 보여준다. 없으면 기본 이미지로 폴백.
-- =====================================================================

ALTER TABLE trips
    ADD COLUMN destination_photo VARCHAR(500) NULL AFTER destination_lng;
