-- =====================================================================
-- V22: 마이페이지 환경설정 — 기본 출발지.
--  AI 일정 생성의 '출발지' 기본값(지금은 화면에 "서울" 하드코딩)을 사용자별로 저장한다.
-- =====================================================================

ALTER TABLE users
    ADD COLUMN default_origin VARCHAR(50) NULL AFTER nickname;
