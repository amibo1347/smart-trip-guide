-- =====================================================================
-- V9: AI가 파악한 목적지 도시를 보관 (예약 딥링크/지도에 활용).
-- =====================================================================

ALTER TABLE plans ADD COLUMN destination_city VARCHAR(100) NULL AFTER ai_model;
